# Bounded per-sample abort/refill investigation

Status: draft investigation in progress.

Reference: radixark/miles#2800 (read-only; parked upstream).

This report is an operator-scoped, opt-in investigation. It does not change production defaults or enable a global refill policy.

